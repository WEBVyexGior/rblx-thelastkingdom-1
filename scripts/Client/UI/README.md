# scripts/Client/UI

Client **UI modules** (per-screen logic/components) managed by `UIController`. Synced to
`StarterPlayer.StarterPlayerScripts.Client.UI` by both place project files.

Actual screens/layouts (parchment death panel, Hub menus, field HUD, ...) are built in the
UI phase.

**Empty in Phase 1 by design** (no UI is built yet). (This README is ignored by Rojo.)
